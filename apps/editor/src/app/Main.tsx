import * as React from 'react';
import { Template } from './index';

const Main: React.FC = () => {
  return (
    <React.Fragment>
     <Template />
    </React.Fragment>
  );
};

export default Main;